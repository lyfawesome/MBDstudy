# MBDstudy Agent Guide

本项目是一个长期、多轮迭代的多体动力学学习与编程求解项目。Agent 的职责不是一次性给出答案，而是持续指导学习、维护代码骨架、推动概念从数学推导落到可运行求解器。

## 核心目标

帮助学习者深入掌握多体动力学及其 Python 编程求解，重点包括：

1. 从物理假设和数学方程出发，保证每个知识点有扎实的数理基础。
2. 从最基础、最重要的概念开始，讲清物理本质、数学本质和工程应用。
3. 每学习一个新知识点，都将其代码化，逐步形成可集成、可扩展的求解器。
4. 为每个示例配套可视化工具和趋势分析工具。
5. 根据学习者反馈不断迭代学习顺序。

## 每讲固定结构

每个知识点都按以下结构组织：

1. 物理假设：明确忽略了什么、保留了什么。
2. 物理本质：解释这个概念在真实世界中表达什么。
3. 数学本质：写出变量、方程、矩阵或约束的精确定义。
4. 工程应用：说明这个概念在机构、车辆、机器人、航天、仿真软件中的用途。
5. Python 实现：把概念加入 `mbd/` 求解器代码。
6. 示例验证：在 `examples/` 中提供最小可运行示例。
7. 趋势分析：检查能量、约束误差、动量、轨迹或数值稳定性。

## LaTeX 公式规范

本项目高度依赖数学公式的正确显示。所有 Markdown 文档必须认真区分行内公式和行间公式。

- 行内短公式使用 `$...$`，例如 `$m\boldsymbol{a}=\boldsymbol{f}$`，以兼容 VS Code 内置 Markdown 预览。
- 行间公式使用 `$$...$$`，公式单独成段，不要放进代码块。
- 变量说明、带分式的公式、矩阵、向量组、约束方程等容易影响预览效果的内容，优先使用行间公式，然后在下一行写中文解释。
- 不要用反引号包裹数学变量或数学方程；反引号只用于代码标识符、文件路径、命令和 API 名称。
- 向量使用 $\boldsymbol{r}$、$\boldsymbol{v}$、$\boldsymbol{a}$，矩阵使用大写粗体或明确符号，如 $\boldsymbol{M}(\boldsymbol{q})$。
- 微分写法优先使用 $\dot{\boldsymbol{r}}$、$\ddot{\boldsymbol{r}}$ 或 $\frac{d\boldsymbol{y}}{dt}$，避免把数学表达式写成纯文本。
- 函数名和物理量下标可用 `\mathrm{}`，例如 $E_{\mathrm{kin}}$、$E_{\mathrm{pot}}$。
- 多行推导使用 `aligned`：

$$
\begin{aligned}
\dot{\boldsymbol{r}} &= \boldsymbol{v}, \\
\dot{\boldsymbol{v}} &= \frac{1}{m}\boldsymbol{f}.
\end{aligned}
$$

- 代码中的变量名可以是 `position`、`velocity`、`mass`，但文档解释中的数学对象应写成 LaTeX。
- 修改文档时必须顺手检查公式渲染是否适合阅读，尤其是上下标、转置、向量和矩阵维度。

## 代码原则

- 优先写清晰、可教学、可扩展的代码。
- 不为了抽象而抽象；只有当多个示例真的需要共享结构时才抽象。
- 数值代码要尽量显式表达数学方程。
- 示例必须可以独立运行。
- 可视化应尽量少依赖外部库；若使用第三方库，需要说明原因。
- 每次新增功能后都运行对应示例验证。

## 案例代码文档规范

每个可运行案例除了理论文档外，必须配套一份代码实现文档，文件名建议使用 `docs/lesson_xx_case_name_code.md`。

代码实现文档必须说明：

1. 理论公式和代码对象的对应关系。
2. 每个核心函数或类承担的数学角色。
3. 状态向量、矩阵、数组切片的维度含义。
4. 不常见 Python 语法的作用，例如 `dataclass`、`Callable`、闭包、切片、`if __name__ == "__main__"`。
5. 数据流如何从初始条件进入求解器，再到分析和可视化。
6. 代码结果用什么物理量或数值趋势验证。

实现文档中的公式仍然遵守 LaTeX 规范：短行内公式使用 `$...$`，复杂公式使用 `$$...$$` 单独成段。

## 当前学习路线

当前从最小动力学闭环开始：

1. 质点 Newton 方程。
2. 二阶方程转一阶状态空间。
3. 固定步长积分器。
4. 能量趋势分析。
5. 广义坐标与自由度。
6. 约束方程与 Lagrange 乘子。
7. 刚体平动与转动。
8. 多体系统装配。
9. DAE、约束稳定化、接触和摩擦。

详细路线见 `docs/roadmap.md`。

## 工作区约定

- `docs/`：学习笔记、推导和路线图。
- `mbd/`：可复用求解器代码。
- `examples/`：每讲对应的可运行示例。
- `outputs/`：示例生成的图像、趋势图和结果文件。

## 协作方式

Agent 应主动推进学习闭环，但学习顺序需要和学习者持续沟通。如果学习者对某个概念不稳，应放慢速度，用更基础的物理图像、数学推导和小代码示例重新解释。

每次完成一个知识点后，应明确回答：

- 这个概念解决了什么问题？
- 它在方程里是什么？
- 它在代码里变成了什么对象或函数？
- 我们用什么结果验证它是可信的？
